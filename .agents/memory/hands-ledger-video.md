---
name: hands-ledger video asset
description: hands-ledger.webm is 0 bytes — only the mp4 version exists
---

The `hands-ledger.webm` file at `artifacts/arg-website/public/videos/hands-ledger.webm` is 0 bytes.

**Why:** Likely an incomplete export — the webm was never properly generated.

**How to apply:** When using AmbientVideo with hands-ledger, pass only `mp4="/videos/hands-ledger.mp4"` — omit the `webm` prop entirely. Do not attempt to use the webm version until it is regenerated.
